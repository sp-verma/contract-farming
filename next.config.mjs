/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'encrypted-tbn0.gstatic.com',
      'img.freepik.com',
      'help.sana-commerce.com',
      'image.vietnamnews.vn'
    ],
  },
  reactStrictMode: true, // Enable strict mode if needed
  // Other configurations if needed
};

export default nextConfig;
